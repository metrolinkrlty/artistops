// Prisma client stub — connect a PostgreSQL DATABASE_URL and run
// `npx prisma generate` + `npx prisma migrate dev` to activate.
// All pages currently use mock-data.ts; swap imports here when ready.

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const prisma: any = null;
